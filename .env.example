# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic Claude API Configuration (Optional - for faster Q&A generation)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Google Gemini API Configuration (Optional - has free tier)
GEMINI_API_KEY=your_gemini_api_key_here

# Server Configuration
PORT=3001
NODE_ENV=production

# LLM Priority (optional, auto-detected based on available keys)
# The system will try LLMs in this order: Claude -> Gemini -> OpenAI
# - Claude 3.5 Sonnet: Fastest, most expensive ($3/1M input tokens)
# - Gemini 2.0 Flash: Fast, has free tier (15 RPM free)
# - OpenAI gpt-4o-mini: Fallback, moderate speed
